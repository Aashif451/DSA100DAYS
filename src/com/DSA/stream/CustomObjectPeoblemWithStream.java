package com.DSA.stream;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;

public class CustomObjectPeoblemWithStream {
	public static void main(String[] args) {
		
//		List<Employee>  empList=List.of(
//				new Employee(101,10000d,"john","IT",30),
//				new Employee(102,12000d,"Saleem","DEV",20),
//				new Employee(103,80000d,"Rizwan","IT",25),
//				new Employee(104,35000d,"Gulshan","R&D",45),
//				new Employee(105,45000d,"Aashif","DEV",25)
//				);
//		
////		Find the student with the highest score from a list of Student objects using streams.
//		empList.stream().max(Comparator.comparingDouble(Employee::getSalary)).ifPresent(e->System.out.println(e.getName()));
//		
//	 List<String> collect = empList.stream().filter(emp->emp.getDepartment().equalsIgnoreCase("IT")).
//		map(Employee::getName).collect(Collectors.toList());
//	 
//	 collect.stream().forEach(s->System.out.println(s));
		
	}
	
	
	
	
	

//
//	Filter employees by department from a list of Employee objects using streams.
//
//	Group students by their grade from a list of Student objects using streams.
//
//	Calculate the average salary of employees using streams.
//
//	Sort students by their name alphabetically using streams.
//
//	Extract distinct departments from a list of Employee objects using streams.
//
//	Check if any employee has a salary above a specified threshold using streams.
//
//	Convert a list of students into a map with student ID as key using streams.
//
//	Count employees with experience greater than 5 years using streams.
//
//	Find the first student who scored above 90 using streams.
	
	
	

}
